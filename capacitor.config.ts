import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pt.ihm.trabalho',
  appName: 'TugaKidsClothes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
